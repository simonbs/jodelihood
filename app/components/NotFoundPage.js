var React = require('react');

var NotFoundPage = React.createClass({  
  render: function() {
    return (
      <div>
      <p>Not found</p>
      </div>
    )
  }
});

module.exports = NotFoundPage;
